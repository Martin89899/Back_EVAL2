version: '3.8'
services:
  backend:
    build: .
    container_name: backend_container
    ports:
      - "5000:5000" # Ajusta el segundo número al puerto que use tu server.js
    environment:
      - DATABASE_HOST=db_container
      - DATABASE_USER=root
      - DATABASE_PASSWORD=root_password
    restart: always